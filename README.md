# Homakesavadurgaprasad Portfolio — Images Fixed

The workflow and marketing campaign images are embedded directly in `index.html`.
No separate image folder is required.

Upload the following to the root of your GitHub Pages repository:

- `index.html`
- `css`
- `js`
- `README.md`

After committing the files, refresh GitHub Pages. A hard refresh may be needed:

- Windows: `Ctrl + F5`
- Mac: `Command + Shift + R`
